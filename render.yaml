services:
  - type: web
    name: meuprojetophp
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: meuprojetodb
          property: connectionString

databases:
  - name: meuprojetodb
    plan: free
